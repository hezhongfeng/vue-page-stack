const COMPONENT_NAME = 'VuePageStack';

const NAVIGATION_ACTIONS = {
  push: 'push',
  replace: 'replace',
  back: 'back',
  forward: 'forward'
};

export { COMPONENT_NAME, NAVIGATION_ACTIONS };

export default {
  COMPONENT_NAME,
  NAVIGATION_ACTIONS
};
