const COMPONENT_NAME = 'VuePageStack';

const NAVIGATION_ACTIONS = {
  push: 'push',
  replace: 'replace',
  back: 'back',
  forward: 'forward'
};

const STACK_EVENTS = {
  back: 'back',
  forward: 'forward'
};

export { COMPONENT_NAME, NAVIGATION_ACTIONS, STACK_EVENTS };

export default {
  COMPONENT_NAME,
  NAVIGATION_ACTIONS,
  STACK_EVENTS
};
